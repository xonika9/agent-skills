<p align="center">
  <img src="assets/x9-agent-skills-hero.jpg" alt="" width="100%">
</p>

<p align="center">
  Язык: <a href="README.md">English</a> · <strong>Русский</strong>
</p>

# x9 Agent Skills

[![Открыть x9 Agent Skills на skills.sh](https://skills.sh/b/xonika9/agent-skills)](https://skills.sh/xonika9/agent-skills)
[![Статус проверки репозитория](https://github.com/xonika9/agent-skills/actions/workflows/validate.yml/badge.svg)](https://github.com/xonika9/agent-skills/actions/workflows/validate.yml)
[![Latest release](https://img.shields.io/github/v/release/xonika9/agent-skills?label=release)](https://github.com/xonika9/agent-skills/releases/latest)

Практические [Agent Skills](https://agentskills.io/) для задач, которые редко укладываются в один промпт: исследования с проверяемыми источниками, работа в браузере под контролем, критика идей и надёжные многошаговые процессы.

- Проверяй актуальные факты, а не полагайся на память модели.
- Работай в авторизованном браузере предсказуемо и отдельно от своих вкладок.
- Заранее задавай автономным процессам точки проверки, условия остановки и требования к доказательствам.

Префикс `x9-` помогает быстро найти скиллы и не путать их с одноимёнными пакетами. В Claude Code/Codex достаточно ввести `/x9`, чтобы увидеть установленные скиллы плагина.

> Полевыми заметками об AI-моделях и инструментах для разработчиков я делюсь в телеграм-канале [«Контролируемые галлюцинации»](https://t.me/+DOZWlhI4r4EyYjgy).

## Установка

Начни с интерактивного установщика. В нём можно выбрать скиллы, нужных агентов и область установки:

```bash
npx skills add xonika9/agent-skills
```

Некоторые процессы объединяют несколько скиллов. При выборочной установке добавь также связанные скиллы из каталога: установщик не разрешает эти зависимости автоматически.

Посмотреть каталог без установки:

```bash
npx skills add xonika9/agent-skills --list
```

Если нужен весь набор как управляемый пакет, используй нативный способ установки для своего агента.

<details>
<summary><strong>Плагин Claude Code</strong></summary>

```bash
claude plugin marketplace add xonika9/agent-skills
claude plugin install x9-agent-skills@xonika9
```

Скиллы будут доступны через namespace плагина, например `/x9-agent-skills:x9-research`.

</details>

<details>
<summary><strong>Плагин Codex</strong></summary>

```bash
codex plugin marketplace add xonika9/agent-skills
codex plugin add x9-agent-skills@xonika9
```

</details>

Клонировать или форкать репозиторий стоит только в том случае, если ты хочешь вести собственные варианты. Обновления из исходного репозитория тогда придётся переносить самостоятельно.

## Совместимость

Скиллы основаны на открытом формате Agent Skills и упакованы как плагины для Claude Code и Codex. Большинство также работает в других совместимых агентах. Исключения, зависящие от runtime, указаны в каталоге и внутри самих скиллов.

## С чего начать

Необязательно сразу разбираться во всём наборе. Найди задачу, которая больше всего похожа на твою:

- Агент отвечает по памяти или делает поверхностный ресерч: начни с [`x9-research`](skills/x9-research/SKILL.md).
- Для задачи важны твой аккаунт, регион, лента, корзина или приватные страницы: добавь [`x9-browser-session`](skills/x9-browser-session/SKILL.md).
- Выбираешь товар на Wildberries: используй [`x9-wb-product-search`](skills/x9-wb-product-search/SKILL.md).
- Хочешь проверить идею до того, как вкладывать в неё время: вызови [`x9-idea-critic`](skills/x9-idea-critic/SKILL.md).
- Нужен хороший промпт или свежий взгляд на уже написанные инструкции: используй [`x9-agent-instructions`](skills/x9-agent-instructions/SKILL.md).
- Новому или уже существующему репозиторию не хватает файлов AGENTS.md и CLAUDE.md: запусти [`x9-context-files-generator`](skills/x9-context-files-generator/SKILL.md).
- Постоянно повторяешь один и тот же многошаговый процесс вручную: спроектируй его через [`x9-loop-engineering`](skills/x9-loop-engineering/SKILL.md).
- Хочешь превратить процесс в скилл или проверить уже готовый: используй [`x9-skill-creator`](skills/x9-skill-creator/SKILL.md).
- Нужна диаграмма Excalidraw, Mermaid или draw.io: используй [`x9-diagrams`](skills/x9-diagrams/SKILL.md).
- Работаешь в Claude Code, но часть задачи хочешь передать Codex: используй [`x9-codex-delegation`](skills/x9-codex-delegation/SKILL.md).
- Markdown-документация разрослась в базу знаний: адаптируй её через [`x9-okf-docs`](skills/x9-okf-docs/SKILL.md).

## Каталог скиллов

### Исследования и личная автоматизация браузера

#### [`x9-research`](skills/x9-research/SKILL.md)

Агенты часто выдают правдоподобный ответ по памяти и называют это исследованием. Этот скилл заставляет их открывать актуальные источники, проверять утверждения, от которых зависит вывод, искать опровергающие данные и честно отмечать то, что подтвердить не удалось. X/Twitter он изучает через твой авторизованный браузер, а Reddit и форумы использует как источник живого опыта, но не как доказательство фактов.

Обычная просьба провести исследование закончится ответом в чате. Если попросишь сохранить результат или создать его в папке, скилл запишет рабочий Markdown-файл в подходящий тематический раздел `docs/research/`. При обновлении он найдёт существующий файл по метаданным OKF, освежит доказательную основу и автоматически пересоберёт HTML с тем же именем, если такой файл уже лежит рядом. Явная просьба об HTML создаст или обновит эту читательскую версию: у пары будет общее имя, но разная подача материала. За работу с браузером отвечает `x9-browser-session`.

#### [`x9-browser-session`](skills/x9-browser-session/SKILL.md)

Для проверки публичного сайта хватает чистого браузера для автоматизации. Для личной задачи он не подходит: результат может зависеть от твоего аккаунта, региона, сохранённых данных или ленты. Скилл выбирает между connector, чистым браузером и отдельным авторизованным Chromium-профилем, а затем работает в собственной вкладке и не забирает твои.

В [инструкции по настройке](skills/x9-browser-session/references/setup.md), которую можно передать агенту, есть проверенный адаптер для Edge на macOS и общий контракт Chromium/CDP. Для локальной веб-разработки и явного запроса оба рантайма используют свой встроенный браузер, если целевой домен не входит в расширяемый список MCP. Домены из списка, пока это только `avito.ru`, открываются через `chrome-devtools` MCP в обоих рантаймах и обходят браузерное расширение. Остальные задачи в Codex начинаются с расширения Edge, а в Claude Code - с MCP. `agent-edge` у обоих остаётся последним резервом на случай, когда пользователь не работает в Edge.

Все обращения к Avito внутри одной родительской задачи идут последовательно. Агент повторно использует уже собранные данные, один раз через пять секунд проверяет временный экран безопасности и возвращает `DEGRADED`, не переключая контроллеры, если ограничение или проверка доступа не исчезли.

#### [`x9-wb-product-search`](skills/x9-wb-product-search/SKILL.md)

Рейтинг на Wildberries редко показывает всю картину: отзывы могут относиться к другому варианту, продавец бывает сомнительным, а цена зависит от аккаунта и региона. Скилл ищет товары через твою авторизованную сессию, проверяет нужный вариант, продавца, цену за единицу, свежие и низкие оценки, повторяющиеся жалобы и фотографии покупателей, когда важны внешний вид или комплектация. Только после этого собирает короткий список.

Он работает только с Wildberries и выполняется внутри текущей сессии Claude Code или Codex. Устанавливай вместе с `x9-browser-session`.

### Идеи и поведение агента

#### [`x9-idea-critic`](skills/x9-idea-critic/SKILL.md)

Этот скилл нужен, когда хочется услышать критику, а не продолжение бодрого брейншторма. Он передаёт нейтральный brief независимым критикам на Opus и GPT. Затем основной агент сводит совпадения и разногласия, смертельные допущения, более дешёвые альтернативы и быстрые тесты, которые могут зарубить идею до больших затрат.

После разбора основной агент переписывает идею в цельную, более жизнеспособную версию и показывает, какое замечание закрывает каждое существенное изменение. По умолчанию запускается по одному критику от каждого провайдера. Есть режимы с одним выбранным критиком и более глубокая панель. GPT-маршрут из Claude Code использует `x9-codex-delegation`.

#### [`x9-agent-instructions`](skills/x9-agent-instructions/SKILL.md)

Это скилл для запроса «напиши мне промпт под эту задачу». Можно своими словами, в том числе через Speech-to-Text, описать нужный результат и получить готовый brief: цель, ограничения, источники, полномочия, результат и понятный критерий готовности.

Он также проверяет уже написанные промпты и инструкции: любой файл, где они лежат, включая глобальные `AGENTS.md` и `CLAUDE.md`. Сначала скилл ничего не меняет. Для каждой предложенной правки он коротко объясняет пользу и существенные последствия, а затем показывает самый небольшой полный дифф. К редактированию переходит только после явного одобрения. Идея простая: умной модели важно полностью описать задачу и объяснить, что считать хорошим результатом, а не расписывать, какими шагами идти.

Основано на:

- [Prompting best practices](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices)
- [Prompting Claude Fable 5](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5)
- [Prompting Claude Opus 5](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5)
- [The new rules of context engineering for Claude 5 generation models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models)
- [Using GPT-5.6](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-5.6)

### Визуальные материалы

#### [`x9-diagrams`](skills/x9-diagrams/SKILL.md)

Выбор вида диаграммы и выбор её файлового формата решают разные задачи. Скилл сначала определяет, какое отношение должен объяснить визуальный материал, а затем выбирает Excalidraw, Mermaid или draw.io под способ передачи результата. Общий метод задаёт вопрос, аудиторию, направление чтения, иерархию, плотность, границы, подписи, маршруты связей и момент, когда перегруженную диаграмму пора разделить.

Каждый маршрут создаёт нативный редактируемый исходник: JSON-файл `.excalidraw` с проверенными связями и актуальными шрифтами, текст Mermaid с проверкой в целевом средстве визуализации либо XML-файл `.drawio` со страницами, слоями, контейнерами, геометрией и разрешимыми связями. Структурная корректность и качество изображения проверяются отдельно, поэтому для завершения нужен просмотр в совместимом редакторе или после визуализации. Если такая проверка недоступна, результат помечается как `DEGRADED`.

### Репозитории и повторяемые процессы

#### [`x9-context-files-generator`](skills/x9-context-files-generator/SKILL.md)

В новом репозитории скилл создаёт полезные `README.md`, `AGENTS.md` и `CLAUDE.md`. В существующем сначала изучает реальные команды, структуру, CI и локальные ограничения. Поэтому на выходе получается рабочий контекст, а не сгенерированное дерево файлов и набор советов, которые агент и так мог вывести из кода.

В личных кросс-харнесс репозиториях источником истины остаётся `AGENTS.md`, а `CLAUDE.md` подключает его через `@AGENTS.md`. Claude Code и харнессы с поддержкой `AGENTS.md` получают один контекст, который не расходится между двумя копиями.

Текст машинных инструкций скилл проверяет через обязательный `x9-agent-instructions`, не копируя его правила. В полном плагине оба скилла уже установлены. Если копируешь `x9-context-files-generator` отдельно, установи их вместе. Без второго скилла проверка репозитория и структуры продолжит работать, но качество инструкций будет отмечено как неполностью проверенное. Для работы только с `README.md` он не требуется.

#### [`x9-skill-creator`](skills/x9-skill-creator/SKILL.md)

Передай ему уже понятный повторяющийся процесс, чтобы упаковать его в Agent Skill, или готовый скилл, чтобы провести аудит. Если замысел пока сырой, он задаст только те вопросы, которые действительно влияют на устройство результата. Затем соберёт или исправит triggers, структуру, references, runtime adapters, границы безопасности и проверки. Если внутри процесса есть сложный автономный цикл, сначала спроектируй его через `x9-loop-engineering`.

Метод применяет единый контракт качества к скиллам для Claude Code и Codex. У структурной проверки есть отдельные профили для переносимого формата, Claude Code и Codex, поэтому результат `PASS` всегда показывает, какую совместимость удалось подтвердить. Можно ограничиться статическим аудитом или добавить поведенческие проверки в чистом контексте, когда такая уверенность оправдывает затраты.

Текст инструкций он проверяет через обязательный скилл `x9-agent-instructions`, не копируя его правила. В полном плагине оба скилла уже установлены. Если копируешь их по одному, ставь эту пару вместе. Без второго скилла структурная проверка продолжит работать, но оценка качества инструкций будет неполной.

Постоянные отчёты в `docs/skill-audits/` скилл пишет на языке, который явно выбрал пользователь. Если язык не задан, он ориентируется на основной язык запроса, а если запрос неоднозначен - на язык текущего диалога.

#### [`x9-loop-engineering`](skills/x9-loop-engineering/SKILL.md)

Некоторые процессы постоянно повторяют один цикл: разобраться, составить план, выполнить работу, проверить, исправить и сохранить выводы. Вместо ручной подачи следующего промпта скилл проектирует ограниченный агентный loop с сохраняемым состоянием, правами на решения, точками проверки, условиями остановки и восстановлением после сбоев.

После проверки цикла `x9-skill-creator` может упаковать его в один скилл. Готовый workflow не требует постоянной подачи следующего промпта, но его запуск всё равно зависит от выбранного runtime, а полномочия и условия остановки остаются ограниченными.

#### [`x9-codex-delegation`](skills/x9-codex-delegation/SKILL.md)

Это узкий мост для передачи хорошо ограниченной задачи из Claude Code в Codex. После выполнения Claude проверяет настоящий diff или собранные доказательства, а не принимает отчёт другого агента на веру. Такой маршрут полезен, когда Codex лучше подходит для отдельной части работы, и помогает распределять лимиты подписок.

Скилл работает только в Claude Code. Он не управляет квотами сам и не передаёт мелкие задачи в другой агент просто ради дополнительного шага.

### Базы знаний

#### [`x9-okf-docs`](skills/x9-okf-docs/SKILL.md)

Большую коллекцию Markdown-документов проще читать людям и агентам, когда каждый файл одинаково описывает себя. Скилл ведёт базу знаний по [OKF v0.2](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md), не привязывает инструкции репозитория к номеру версии и постепенно обновляет только те документы, с которыми идёт работа. При первой записи в старом репозитории он также убирает из `AGENTS.md` скопированные правила версии или прежнее имя `x9-okf-adapt`.

Основной файл скилла теперь только коротко выбирает маршрут, поэтому при обычном редактировании загружаются лишь правила сопровождения. Внедрение, исправление и массовая миграция вынесены в отдельные справочные файлы. Старые коллекции 0.1 работают без обязательного общего переноса, а скрипты проверяют неизменность основного текста, окончаний строк и BOM.

## Глобальные файлы инструкций

В репозитории лежат отдельные готовые глобальные файлы для [OpenCode](global-files/opencode/AGENTS.md), [Codex](global-files/codex/AGENTS.md) и [Claude Code](global-files/claude/CLAUDE.md). Общая часть у них одна, а инструкции для каждого harness хранятся отдельно. Они охватывают язык общения, границы полномочий, сохранение чужих изменений, работу с неопределённостью, проверяемый критерий готовности, выбор документационных и браузерных инструментов и проверенное поведение субагентов. Машинные пути заменены на обнаружение skills по имени.

Считай эти файлы примерами, а не готовой заменой своей конфигурации. Безопасный запрос выглядит так:

```text
Используй x9-agent-instructions: изучи эти примеры и добавь только подходящие мне правила.
Сохрани мои текущие инструкции, пути, инструменты и репозиторные разделы.
```

## Обновление

Обновить один глобально установленный скилл:

```bash
npx skills update x9-research -g
```

Обновить все глобально установленные скиллы:

```bash
npx skills update -g
```

Плагины обновляются через marketplace соответствующего агента. Если твой процесс зависит от особенностей runtime, перед обновлением проверь [CHANGELOG.md](CHANGELOG.md).

## Участие в проекте и безопасность

Можно присылать баг-репорты, точечные улучшения скиллов и воспроизводимые наблюдения о совместимости. Перед выпуском CI проверяет каждый скилл, plugin metadata, регрессионные тесты и публичные файлы. Перед pull request прочитай [CONTRIBUTING.md](CONTRIBUTING.md). Об уязвимостях и других чувствительных проблемах сообщай по инструкции в [SECURITY.md](SECURITY.md), а не через публичный issue. Правила общения собраны в [Code of Conduct](CODE_OF_CONDUCT.md).

Если набор экономит тебе время, [поставь репозиторию звезду](https://github.com/xonika9/agent-skills), чтобы его нашли другие. Новые эксперименты и практические заметки выходят в канале [«Контролируемые галлюцинации»](https://t.me/+DOZWlhI4r4EyYjgy).

## Лицензия

[MIT](LICENSE). Можно брать отдельные скиллы, менять их под себя и собирать собственный набор.
